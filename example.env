# CONFIGURACIÓN SEGURA - Nutri Gemini
# IMPORTANTE: Copia este archivo como .env y configura tus propias credenciales
# NUNCA subas el archivo .env al repositorio

# =============================================================================
# VARIABLES REQUERIDAS - Debes configurar TODAS estas variables
# =============================================================================

# API Key de Google Gemini AI (REQUERIDA)
# Obtén tu API key desde: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=tu_google_api_key_aqui

# Clave secreta de Django (REQUERIDA)
# Genera una nueva clave desde: https://djecrety.ir/
SECRET_KEY=tu_secret_key_de_django_aqui

# 🗄️ URL completa de base de datos PostgreSQL (REQUERIDA)
# Formato: postgresql://usuario:contraseña@host:puerto/nombre_bd
# Ejemplo: postgresql://postgres:password123@localhost:5432/nutricion_db
DATABASE_URL=postgresql://usuario:contraseña@host:puerto/nombre_bd

# =============================================================================
# ⚙️  CONFIGURACIÓN OPCIONAL
# =============================================================================

# Modo debug (True para desarrollo, False para producción)
DEBUG=True

# =============================================================================
# VARIABLES INDIVIDUALES DE BD (Opcional - solo si no usas DATABASE_URL)
# =============================================================================
# Estas variables son alternativas a DATABASE_URL para configuración individual

# DATABASE_NAME=nombre_de_tu_base_de_datos
# DATABASE_USER=tu_usuario_postgresql
# DATABASE_PASSWORD=tu_contraseña_postgresql
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# =============================================================================
# INSTRUCCIONES DE CONFIGURACIÓN
# =============================================================================
#
# 1. GOOGLE_API_KEY:
#    - Ve a https://makersuite.google.com/app/apikey
#    - Inicia sesión con tu cuenta de Google
#    - Crea una nueva API key
#    - Copia y pega la clave en GOOGLE_API_KEY
#
# 2. SECRET_KEY:
#    - Ve a https://djecrety.ir/
#    - Copia la clave generada
#    - Pégala en SECRET_KEY
#
# 3. DATABASE_URL:
#    - Si usas Railway, Heroku, u otro proveedor, copia la URL completa
#    - Si usas PostgreSQL local, usa el formato mostrado arriba
#
# 4. GUARDAR Y PROTEGER:
#    - Guarda este archivo como '.env' (sin el 'example')
#    - NUNCA subas el archivo .env al repositorio
#    - El archivo .env está en .gitignore por seguridad
#
# =============================================================================

